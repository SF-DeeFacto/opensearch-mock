# opensearch docker 파일
docker-compose up -d
docker-compose logs opensearch-node
docker-compose down

# python 가상환경 생성 및 필요한 모듈 설치
python -m venv venv
.\venv\Scripts\activate
pip install pandas opensearch-py
python 파일명.py
deactivate

# python 파일 선택
stream_csv_to_opensearch: 센서 5개 데이터 (local)

# Grafana (cmd 관리자 권한 실행)
net stop Grafana
net start Grafana
(admin/admin)
